"""
src/infrastructure/adapters

Concrete InfrastructureAdapter implementations.

Each adapter file auto-registers itself on import.
To add a new environment, create a new file here that calls
register_adapter(YourAdapter()) at module level.
"""
